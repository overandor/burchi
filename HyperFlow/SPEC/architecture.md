# Architecture Specification

## System Architecture
Multi-agent control plane with receipt-backed verification.

## Acceptance Tests

- `test -d scripts/`
- `test -d tests/`
- `test -d audits/`
- `test -f verify.sh`
